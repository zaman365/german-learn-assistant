# Finish German Learn Assistant — prompt for Codex or Claude Code

You are the coding agent for https://github.com/zaman365/german-learn-assistant. Continue the existing implementation through its remaining educational, exam, audio and release acceptance gates. Make working code and content changes, verify them, and push reviewable commits. Do not stop after an audit or plan.

## Starting point and contract

The open draft is PR #1, branch codex/build-learning-copilot. Its last verified remote commit before this handoff was 38d587a687ea9a5b5d9e03791651b7bf80f81feb. Check the **current** remote and working tree before editing. A prior workspace had local commits with the same file tree but different ancestry from the remote branch: preserve all work, use a clean checkout or isolated worktree from the remote branch if needed, and do not force-push or reset main.

Read BUILD_SPEC.md, docs/COURSE_SPEC.md, docs/CURRICULUM_MAP.md, docs/DTB_C1_EXAM_BLUEPRINT.md, docs/GRAMMAR_REFERENCE.md, docs/C1_FILES_REVIEW.md, docs/CONTENT_COVERAGE.md, docs/IMPLEMENTATION_STATUS.md and docs/RUNBOOK.md in full. The user's goal is one self-contained route: a comfortable B2-to-C1 bridge, professional C1 instruction and practice, DTB C1 preparation after C1, and a clearly labeled path toward C2. Everyday recommendations must respond to saved evidence. Article rules, grammar, vocabulary, worked examples, diagrams and repairs must be available inside the course. The exam remains **Deutsch-Test für den Beruf C1**.

There is already a runnable Next.js/PostgreSQL app, 89 authored lessons, five diagnostic rounds, 125 native vocabulary drills, three original 60-task mocks, export/import, a worker and a deployment package. Do not replace the app or treat counts as proof that the instruction is complete. The baseline CI run at https://github.com/zaman365/german-learn-assistant/actions/runs/35864493243 passed native PostgreSQL integration, migration/seed, a production build, and authenticated desktop/mobile browser journeys. Local checks had 26 unit and 11 integration tests. The current status file names the remaining gates; inspect it for updates before acting.

## Work to finish

1. **Audit educational coverage and repair real gaps.** Trace each requirement and exit rubric in COURSE_SPEC and CURRICULUM_MAP to actual learner-facing instruction, prerequisite explanation, vocabulary, independent exercise and feedback. Sample the whole course systematically, not just the first lesson in each module. Correct misleading rules, weak examples, missing lexical senses/article/plural information, thin transfer tasks, incomplete speaking themes and links that make the learner leave the course for essentials. Preserve the corrected gender-versus-case distinction and explicit exception patterns. Add useful content rather than titles or near-duplicate templates. Keep content versions immutable once published and update the coverage matrix. Record the method and scope of German-language review; do not claim independent human review unless a qualified reviewer actually performed it.

2. **Finish evidence-based learning behavior.** Compare placement, checkpoint transitions, prerequisites, lesson completion, error recurrence and resolution, independent transfer, seven-day retention, reviews, short-session resumption, weekly balance and the next-day plan with BUILD_SPEC. Repair any incorrect behavior. In particular, ensure an error moves to resolved only after relevant new independent evidence; an ambiguous article error needs a probe, and a placement waiver must be reconsidered when later evidence contradicts it. Keep assisted or recognition-only responses from creating broad productive mastery. Preserve completed daily tasks during replanning, cap overdue review work, and give a reason for the recommendation. Make the promised German UI option work across essential screens as well as English explanations. Verify keyboard access, status labels, reduced motion and layouts at 390, 768 and 1440 px.

3. **Improve the original exam material and simulator fidelity.** Compare all three mocks against the *actual* DTB C1 source structure and task genres in DTB_C1_EXAM_BLUEPRINT. The first reading matching task currently needs newspaper-style teasers rather than generic learning offers; listening matching needs four genuine conversations and six shared statements. Check other text length, dialogue roles, distractors, timing and difficulty with an independent German reviewer. Use original material only; do not commit official test papers or recordings. Preserve the 65-minute combined reading/reply block, continuous 25-minute listening/note block, 45-minute language/statement block and paired oral task structure where the verified paper model applies. Keep the phone-note information aspect all-or-nothing at four points and shared writing/speaking language criteria scored once. Maintain hidden answers, server-authoritative deadlines, owner-scoped recordings, idempotent saves and the reserved unseen set.

4. **Resolve official-rule questions from primary sources.** Recheck current telc and, where relevant, BAMF information for the specific DTB C1 exam and applicable date. Verify the exact 36-point pass threshold, task-specific scoring, administrative/navigation rules and the announced digital transition before publishing an official pass/fail evaluator or verified simulation. Version the definition and cite source/page/date for every change. If official evidence remains ambiguous or unavailable, keep the affected rule marked unresolved and the official verdict/simulation unavailable; continue useful original section practice and clearly labeled coaching scores. Never fabricate a pass probability or assume that a paper interface specifies the future digital interface.

5. **Prove real AI and audio paths when credentials/services are available.** Verify the configured text model with representative German writing and mediation, a malformed response, refusal, timeout, retry and saved-attempt recovery. Verify German transcription separately from *actual audio* analysis, then speech synthesis and a recorded oral partner turn. Confirm evidence excerpts, rubric indices, timestamps, quota behavior and owner isolation. Generate and listen to course clips and all mock clips, especially speaker distinctions, names and numbers; review the complete composed 25-minute timelines. Version any revised script or media, and keep unavailable or unreviewed listening tasks locked. Test microphone capture on secure desktop/mobile origins and private playback/deletion. Do not mark a model setting, generated file, transcript or fixture as a successful real sound assessment. Never commit API keys, owner passwords or learner recordings. If access or a paid provider budget is missing, complete independent code/content work and record the precise blocked checks; do not invent success.

6. **Run the deployment and recovery gates.** Exercise the Docker web and worker targets with native PostgreSQL and private S3-compatible storage (a disposable MinIO setup is suitable); verify queue recovery, stale uploads, media retention, signed URL expiry and the standalone package. Take a matching database/object-store backup and restore to a separate staging instance; check owner login, a saved lesson, an exam, export and media deletion afterward. Fix implementation issues revealed by the rehearsal. Prepare a concrete staging deployment configuration and document required secrets. Do not deploy to production, merge the PR, provision paid infrastructure or send messages on the user's behalf without authorization.

## Verification and handoff

Run pnpm install --frozen-lockfile, pnpm content:validate, pnpm typecheck, pnpm lint, pnpm format:check, pnpm test, pnpm test:integration, pnpm build and the desktop/mobile pnpm test:e2e path with native PostgreSQL when available. Add focused tests only for risks and behaviors you changed. A clean migrate/seed and an idempotent reseed must still work; never silently replace published content versions or lose attempts. Keep GitHub CI green after code changes.

Update docs/IMPLEMENTATION_STATUS.md with an explicit **implemented / verified with evidence / blocked** entry for each remaining gate and update docs/CONTENT_COVERAGE.md when material changes. Commit and push coherent work to the existing draft PR, verify the remote branch and checks, and summarize what the learner can actually use, what was tested with real services and what still needs access, reviewer input or official clarification. Carry the task forward autonomously; ask the user only for a specific missing credential, paid-service authorization, official ambiguity that cannot be resolved, or final production-release decision after all independent work is complete.
