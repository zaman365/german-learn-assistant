import Link from "next/link";
import { requireUser } from "@/auth/server";
import { dashboard } from "@/learning/service";
export default async function Progress(){const user=await requireUser();const d=await dashboard(user.id);return <><div className="page-heading"><div><span className="eyebrow">YOUR LEARNING RECORD</span><h1 style={{marginTop:12}}>Progress you can point to.</h1><p className="muted">Course completion, demonstrated skill and exam readiness tell different stories.</p></div><Link href="/settings" className="button secondary">Export progress</Link></div><div className="metric-grid"><div className="card metric"><span className="stat-label">Course completion</span><strong>{d.stats.completed} / {d.stats.total}</strong><span className="small muted">Published lessons completed</span></div><div className="card metric"><span className="stat-label">Demonstrated objectives</span><strong>{d.stats.demonstrated} / {d.skills.length}</strong><span className="small muted">Separate-day independent tasks</span></div><div className="card metric"><span className="stat-label">Exam readiness</span><strong style={{fontSize:"1.35rem"}}>Not assessed</strong><span className="small muted">Full valid mock evidence is still needed</span></div></div><div className="dashboard-grid"><div className="stack"><section className="card"><h2>Skill evidence</h2><p className="small muted">Retained means a successful later transfer check, at least seven days after independent demonstration.</p>{d.skills.map(s=><div key={s.id} className="activity"><div className="grow"><h3 style={{textTransform:"capitalize"}}>{s.id.replaceAll("-"," ")}</h3><p className="small muted">{s.evidenceCount} assessed attempts{s.demonstratedAt?` · demonstrated ${s.demonstratedAt}`:""}</p></div><span className={`badge ${s.state==="not_assessed"?"neutral":s.needsRepair?"amber":""}`}>{s.needsRepair?"Needs repair":s.state.replaceAll("_"," ")}</span></div>)}</section><section className="card"><h2>Recent work</h2>{!d.history.length?<p className="muted">Your first attempt will appear here.</p>:d.history.slice(-12).reverse().map(h=><div className="activity" key={h.attempt.id}><div className="grow"><h3>{h.attempt.skill.replaceAll("-"," ")}</h3><p className="small muted">{h.attempt.localDate} · {h.attempt.lessonId} · {h.attempt.assisted?"assisted":"unaided"}</p><p lang="de" className="small" style={{overflowWrap:"anywhere"}}>{h.attempt.response.slice(0,140)}</p></div><span className="badge neutral">{h.evaluation.status!=="completed"?"Awaiting feedback":h.evaluation.data.correct?"Correct":"Repair"}</span></div>)}</section></div><aside className="stack"><section className="card"><h2>Coming up for review</h2>{d.reviews.length?d.reviews.map(r=><div className="activity" key={r.id}><div><h3>{r.targetId.replaceAll("-"," ")}</h3><p className="small muted">{r.mode} · {r.dueDate}</p></div></div>):<p className="muted small">Reviews are scheduled after practice. Reading an explanation alone does not create a successful retrieval.</p>}</section><section className="card"><h2>Your bridge placement</h2>{d.routes.length?d.routes.map(r=><details className="disclosure" key={r.moduleId}><summary className="small">{r.moduleId} · {r.route}</summary><p className="small muted" style={{marginTop:10}}>{r.rationale}</p></details>):<p className="small muted">Complete a diagnostic segment to start building your route.</p>}</section></aside></div></>;}
