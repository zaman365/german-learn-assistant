"use client";
import { useState } from "react";
import { Search } from "lucide-react";
import type { LexicalEntry } from "@/content/types";
export default function VocabularyBrowser({entries}:{entries:LexicalEntry[]}){const[q,setQ]=useState("");const[type,setType]=useState("all");const filtered=entries.filter(w=>(type==="all"||w.type===type)&&`${w.word} ${w.meaning} ${w.phrase}`.toLocaleLowerCase("de").includes(q.toLocaleLowerCase("de")));return <><div className="row" style={{alignItems:"start",flexWrap:"wrap"}}><div className="field search grow"><label className="screen-reader" htmlFor="word-query">Search vocabulary</label><div className="row"><Search size={18}/><input id="word-query" value={q} onChange={e=>setQ(e.target.value)} placeholder="Search a word, meaning or phrase…"/></div></div><select aria-label="Word type" style={{width:160}} value={type} onChange={e=>setType(e.target.value)}><option value="all">All words</option><option value="noun">Nouns</option><option value="verb">Verbs</option><option value="adjective">Adjectives</option></select></div><p className="small muted">{filtered.length} entries · Learn the whole package, then use it.</p><div className="grid-two">{filtered.map(w=><article className="card" key={w.id}><div className="row"><h2 lang="de" style={{margin:0}}>{w.article&&<span style={{color:w.article==="der"?"#486a95":w.article==="die"?"#a55b4c":"#4b7554",marginRight:7}}>{w.article}</span>}{w.word}</h2><span className="badge neutral">{w.type}</span></div><p className="muted small" style={{marginTop:8}}>{w.meaning}</p>{w.plural&&<p className="small" lang="de"><strong>Plural:</strong> {w.plural}</p>}{w.forms&&<p className="small" lang="de">{w.forms}</p>}<p className="small muted"><strong>Stress:</strong> {w.stress}</p><div className="example"><p lang="de">{w.phrase}</p><p lang="de" className="small" style={{marginTop:10}}>{w.example}</p><p className="small muted" style={{marginTop:5}}>{w.translation}</p></div><span className="small muted">{w.register}</span></article>)}</div>{!filtered.length&&<div className="empty">No matching entry in the published vocabulary. Try the German base form or its English meaning.</div>}</>;}
